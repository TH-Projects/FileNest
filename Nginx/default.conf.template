upstream reactapp{
    server ${URL_REACTAPP}:${PORT_REACTAPP};
}

server {
    listen 80;
    server_name ${NGINX_SERVER_NAME};

    # Set the maximum allowed size of the client request body to 10 MB
    client_max_body_size 10M;

    # Proxy for broker-service
    location ~* ^/(getQueue|couple|addQueue|getCouples) {
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_pass http://broker:6001;
        proxy_read_timeout 30;
        proxy_connect_timeout 90;
        proxy_redirect off;
    }

    # Proxy for fastify backend server
    location ~* ^/(download|upload|delete|listObjects|checkAndCreateUser|loginUser){
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_pass http://serverfastify;
        proxy_read_timeout 30;
        proxy_connect_timeout 90;
        proxy_redirect off;
    }

    # Proxy for database server
    location ~* ^/(getFiles|authUser|createUser|addCluster|addFile|addMinIOServer|deleteFile|getClusterForFile|MinIOServer|getFile|checkUserExistance|getAccountIdByUsername|removeMetaInfo|minIOServerForUpload) {
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_pass http://servermetadb;
        proxy_read_timeout 30;
        proxy_connect_timeout 90;
        proxy_redirect off;
    }

}